from django.apps import AppConfig


class HVACValidationConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'air_validation'  # Keep as air_validation for Django app compatibility
